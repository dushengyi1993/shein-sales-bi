{
  "environment": "test",
  "cooperationMode": "半托管",
  "market": "SA",
  "apiBaseUrls": {
    "test": "https://openapi-test01.sheincorp.cn",
    "prodSemiManaged": "https://openapi.sheincorp.com"
  },
  "authHosts": {
    "test": "openapi-sem-test01.dotfashion.cn",
    "prod": "openapi-sem.sheincorp.com"
  },
  "app": {
    "appId": "默认开放平台应用 APP_ID；不要提交真实值",
    "appSecretKey": "默认开放平台应用 APP_SECRET_KEY；不要提交真实值",
    "webhookValidationStoreKey": "共享 App 的技术验证归属店铺，例如 DL；仅用于 appScopedOnly 隔离"
  },
  "apps": {
    "示例应用KEY": {
      "appId": "当不同店铺属于不同开放平台应用主体时，在这里填写该应用 APP_ID；不要提交真实值",
      "appSecretKey": "当不同店铺属于不同开放平台应用主体时，在这里填写该应用 APP_SECRET_KEY；不要提交真实值",
      "webhookValidationStoreKey": "同一个 App 授权多店时必填一个已授权店铺；只隔离平台消息测试"
    }
  },
  "webhookRetiredAppIdSha256": [],
  "stores": [
    {
      "storeKey": "DL",
      "shopName": "示例店铺名",
      "enabled": false,
      "appKey": "示例应用KEY",
      "openKeyId": "店铺授权获得；不要提交真实值",
      "secretKey": "解密后的店铺 secretKey；不要提交真实值",
      "encryptedSecretKey": "平台返回的加密 secretKey，可选；不要提交真实值",
      "authorizedAt": null,
      "notes": "先用一个真实店铺试点；同一 App 可授权 19 店，但每店仍必须保存各自唯一 OpenKey"
    }
  ],
  "safeWriteOperations": {
    "enabled": false,
    "requireDryRun": true,
    "allowedOperations": [],
    "allowedStores": [],
    "notes": "平台动作物理总闸门：只控制哪些店铺+动作具备可靠真实写适配器。人员权限以登录 BI 账号的 writeStores 为唯一事实源；仍必须完成 dry-run、任务快照锁定、用户确认、审计和回读。"
  }
}
