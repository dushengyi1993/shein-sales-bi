{
  "environment": "test",
  "cooperationMode": "半托管",
  "market": "SA",
  "apiBaseUrls": {
    "test": "https://openapi-test01.sheincorp.cn",
    "prodSemiManaged": "https://openapi.sheincorp.com"
  },
  "authHosts": {
    "test": "openapi-sem-test01.dotfashion.cn",
    "prod": "openapi-sem.sheincorp.com"
  },
  "app": {
    "appId": "默认开放平台应用 APP_ID；不要提交真实值",
    "appSecretKey": "默认开放平台应用 APP_SECRET_KEY；不要提交真实值"
  },
  "apps": {
    "示例应用KEY": {
      "appId": "当不同店铺属于不同开放平台应用主体时，在这里填写该应用 APP_ID；不要提交真实值",
      "appSecretKey": "当不同店铺属于不同开放平台应用主体时，在这里填写该应用 APP_SECRET_KEY；不要提交真实值"
    }
  },
  "stores": [
    {
      "storeKey": "DL",
      "shopName": "示例店铺名",
      "enabled": false,
      "appKey": "示例应用KEY",
      "openKeyId": "店铺授权获得；不要提交真实值",
      "secretKey": "解密后的店铺 secretKey；不要提交真实值",
      "encryptedSecretKey": "平台返回的加密 secretKey，可选；不要提交真实值",
      "authorizedAt": null,
      "notes": "先用一个真实店铺试点，跑通后再复制到 16 店"
    }
  ],
  "safeWriteOperations": {
    "enabled": false,
    "requireDryRun": true,
    "allowedOperations": [],
    "allowedStores": [],
    "notes": "第一层物理总闸门：只控制哪些店铺+动作具备真实写适配器。即使这里开启，仍必须同时命中 config/bi_ops_write_whitelist.local.json 的人+店+动作试点白名单。"
  }
}
