[Service]
# 2026-09-15 fnOS VM capacity override: the 2 vCPU cloud host needed 3.
# Held as a drop-in so the tracked unit keeps its portable default; review it
# together with /etc/systemd/system.conf.d/60-shein-bi-host-pressure.conf.
Environment=SHEIN_OPENAPI_RECONCILE_CONCURRENCY=6

