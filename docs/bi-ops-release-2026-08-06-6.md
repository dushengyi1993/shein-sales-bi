# BI Ops 2026.08.06.6

## 修复首页缓存长期不更新

- 根因不是两项经营数据真的生成失败，而是 Portal 队列长期被持续到来的订单、售后和利润高优先级任务占用；`homeTrafficDaily` 与 `priceScatter` 既没有进入每日首页预热，又以普通后台优先级入队，最终一直停留在 2026-08-04 的完整缓存。
- 首页精简流量和订单成交价散点现在随每日 core 一起按首页优先级入队，并加入 Portal 启动/core 换代预热清单。
- 页面“强制刷新最新”现在使用优先级 0，确保先于普通后台重建执行。
- 队列增加等待老化：低优先级项每等待两分钟提升一级有效优先级，但永远不会越过负责人显式强刷，避免任何 section 无限期饥饿。
- 仍保留原子缓存兜底：新数据发布前继续显示上一份完整数据，不把缺失值冒充为 0。

## 验证

- 本机确定性测试 156/156 通过。
- 生产部署后需确认 `homeTrafficDaily.json` 与 `priceScatter.json` 的 `generatedAt` 等于当前 core，并回读 Portal 健康和 watchdog。
