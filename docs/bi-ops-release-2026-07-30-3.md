# SHEIN BI / Ops 2026.07.30.3 发布说明

发布日期：2026-07-30

标签：`2026.07.30.3`

上一正式版本：`2026.07.30.2`

## 本版范围

- 修复 19 店切到 DL 单一 App 后，商品日更仍并发请求全量 `spu-info`、触发共享额度 `832213` 限流的问题。
- 商品列表和库存继续每日全量读取；详情改为每店 16 条轮转、优先重试上次失败项，其余使用 21 天内最近成功详情。
- 近期详情缓存只作为静态详情兜底；实时上下架仍以 Webhook 和当前业务域为准。无详情、库存缺失、缓存超期或无 Webhook 的实时状态回退仍会告警。
- watchdog 增加生产部署标记和源码状态持续检查，自动发现 commit 漂移、源码脏改、`skip-worktree` / `assume-unchanged` 和 tracked 文件缺失。

## 验证

- 本机完整确定性回归：134 / 134。
- 真实云端 19 店商品对账重跑，确认不再触发共享 App 限流并恢复 matched。
- watchdog 重跑、Portal health、Webhook/SSE、销售覆盖、利润 section 和 Git 发布状态回读。

## 部署与回滚

- 生产部署后必须执行 `check_release_source_state.mjs --record-deployment 2026.07.30.3`。
- 源码回滚点：`2026.07.30.2`；OpenAPI 商品快照、数据库和运行态不随源码回滚删除。
