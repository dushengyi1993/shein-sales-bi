# 每日库存自动执行服务状态修复

目标版本：`2026.08.03.9`
发布日期：2026-08-03

## 1. 修复

- 修正自动执行完成后的 jq 汇总表达式括号，避免完整结果已落盘却因日志汇总编译失败而把 systemd 单元标成 failed。
- 不改变库存计划、授权、执行或回读逻辑。

## 2. 验证

- 使用生产完整结果运行汇总表达式，确认返回 `ok=true`。
- 再次触发守卫时识别同 hash 完整结果并走 `already_completed`，不重复执行库存写入。
- 本地、CI、云端测试与 release source state 均通过。
