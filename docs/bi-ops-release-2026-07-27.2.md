# SHEIN BI / Ops 2026.07.27.2 发布说明

发布日期：2026-07-27
Git tag：`2026.07.27.2`
对比基线：`2026.07.27.1`

## 修复内容

- 首页“当前时段订单 / 销量 / 动销”直接展示销量对账式：
  - `总销量 − 已落定退款/履约反转 = 净销量`
  - 未落定售后单独注明“退款待落定，暂不扣减净销量”。
- 首页“当前时段退货 / 售后”的订单数与金额下方，直接拆出“退款已落定”和“退款待落定”，不再要求用户进入子页后自行推算。
- “退货退款”页新增“退款待落定”状态筛选；顶部卡片、时间分析和明细统一使用完整业务名称，不再只写含糊的“待决”。
- 待落定售后继续参与风险展示，但不会提前冲减净销量、净成交额或已落定利润。

## HS-025 线上复核

筛选区间 `2026-05-01` 至 `2026-07-27`：

- 总销量 `23` 件。
- 退款已落定 `2` 件，共 `121.68 SAR`。
- 净销量 `21` 件，即 `23 - 2 = 21`。
- 另有退款待落定 `2` 件，共 `104.91 SAR`，只列风险，暂不扣减净销量。
- 四笔售后合计正好是 `2` 笔已落定加 `2` 笔待落定，不存在少算或重复冲减。

`2026.07.27.1` 发布说明记录的是当时的旧状态；此后 `HL / GSH18M03S00M28F` 已由待决更新为“同意退款”，因此当前线上结果由 `22` 件净销量变为 `21` 件。这是平台售后状态落定后的正常变化，不是公式再次漂移。

## 验证

- 生产数仓直接核对 `mart.profit_order_item_cache` 与 `mart.after_sales_settlement_detail`。
- `npm test`：`122/122` 通过。
- 页面文案和筛选均增加确定性契约测试，防止后续把待落定售后再次隐藏到含糊小字中。
