# 营销运行归档：2026-07-13 至 2026-07-16

本文件保存该时段的执行事实和回读，不是长期业务政策或当天执行授权。当前入口见 [../../marketing-daily-inspection-handoff.md](../../marketing-daily-inspection-handoff.md)。

## 2026-07-13

- 19 店普通活动 live 审核完成；发现 `48217`、`48215`、`48925` 待报活动，执行前须以活动商品页的精确 SKC 去重并回读。
- YJ `KF-JN-02` 新链接已创建限时折扣 `80169840 / 96.27`。DX `SK-999` 的混合旧活动 `79655625` 返回 `0006`，保留安全阻断。
- DX `SK-13065`、YJ `SK-13015`、HL `SK-777` 的人工特殊限时折扣分别恢复为 `80281965 / 283`、`80282780 / 163.75`、`80282425 / 127.29`；均按用户批准价、库存与有效期回读。最终定点 scan 将三条合并回完整 19 店基线，`covered_exact=3`、`actionCount=0`。

## 2026-07-15

- 普通活动 `48217 / 48215 / 48925` 已完成 19/19 店报名和回读，最终基准共 1063 行（`388 / 641 / 34`），`missingRows=0`、`priceMismatchRows=0`、`extraAvailableRows=0`、`activityListGapRows=0`。
- 最终基准为 `selection-plan-2026-07-15-v48217-48215-48925-final-executed-all-1063.json` 及对应 `price-overrides`，标记为后续普通活动和限时折扣兜底基准；活动开始前不得用未来窗口目标审计订单。
- 7 条新上架/重新上架限时折扣完成；DX 混合活动只移除目标 SKC 后，活动 `80600855` 覆盖 3 条，`uncovered=0`、`duplicate=0`。最终 guard 为 19/19、普通目标价漂移 0、人工特殊折扣 14/14 `covered_exact`、blocker 0；仍有 6 个历史/证据 warning，不能表述为永久无风险。

## 2026-07-16

- 已授权的限时折扣兜底可在当天 ET 实盘足够时，将平台虚拟库存精确补到计划 `activityStock`；ET 不足、证据非当天或回读不一致仍 fail closed，且该授权不扩展到普通活动或优惠券。
- LQ `SK-5110`：`80815455 / 61.34 SAR / stock 10`；XL `SK-999`：`80790590 / 107.76 SAR / stock 10`；TS `SK-03038`：`80821115 / 329.59 SAR / stock 10`；DX `SK-270`：`80848625 / 263.57 SAR / stock 10`。均经定点 live readback。
- TS 漏检由旧 BI `linksData` 早于原始链接快照造成；guard 和计划器现在以 `marketing_latest_raw_link_overlay` 补入 BI sidecar 缺失的 `store+SKC`，原始快照不完整时输出 `latest_raw_link_overlay_incomplete` blocker。最终合并快照 699 行、guard `blockers=0`。

## 使用边界

- 当前真实操作必须重新取得当天 live evidence、dry-run、授权、审计和 readback。普通人工任务继续锁定 payload hash；负责人长期授权的云端营销 timer 不逐次索要 hash，但只能执行策略白名单内的限时折扣动作。
- 人工特殊限时折扣的持久事实源始终是 `config/marketing_manual_limited_discount_overrides.json`；本归档只保留当时的执行记录。
